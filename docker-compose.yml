version: "2"
services:
    web:
        build: .
        depends_on:
            - db
        ports:
            - "8888:8888"
        environment:
            - NODE_ENV=production
    db:
        image: nonoroazoro/rpi-mongo
        volumes:
            - ./db:/data/db
